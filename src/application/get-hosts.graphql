query GetHosts {
    hosts {
        id
        name
        description
        uri
        workerCount
        queues {
            hostId
            name
            id
            isPaused
            ...JobCounts
        }
    }
}
